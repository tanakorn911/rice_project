# 🍚 Rice Project

Rice Project เป็นโปรเจกต์ Backend สำหรับระบบที่เกี่ยวข้องกับข้าว  
พัฒนาโดยใช้ Python และรองรับการรันผ่าน Docker เพื่อความสะดวกในการติดตั้งและใช้งาน

---

## 📂 โครงสร้างโปรเจกต์

rice_project/
├─ backend/ # โค้ดฝั่ง Backend
├─ Dockerfile # ไฟล์สำหรับ build Docker image
├─ docker-compose.yml # ตั้งค่าการรันระบบด้วย Docker Compose
├─ requirements.txt # รายการ Python dependencies
└─ README.md # เอกสารอธิบายโปรเจกต์


---

## ✨ Features

- Backend API สำหรับระบบจัดการข้อมูล
- รองรับการรันด้วย Docker
- โครงสร้างโปรเจกต์พร้อมสำหรับต่อยอด
- แยก environment ได้ง่าย

---

## 🛠️ Requirements

- Python 3.8+
- Docker
- Docker Compose

---

## 🚀 วิธีติดตั้งและรันโปรเจกต์

### ▶️ วิธีที่ 1: รันด้วย Docker (แนะนำ)

1. Clone repository
   ```bash
   git clone https://github.com/tanakorn911/rice_project.git
   cd rice_project

    Build และรัน container

docker compose up --build

เปิดใช้งานผ่าน browser

    http://localhost:8000

    หมายเหตุ: port อาจแตกต่างขึ้นกับการตั้งค่าใน backend

▶️ วิธีที่ 2: รันแบบไม่ใช้ Docker

    เข้าโฟลเดอร์ backend

cd backend

สร้าง virtual environment

python -m venv venv
source venv/bin/activate   # Linux / macOS
venv\Scripts\activate      # Windows

ติดตั้ง dependencies
cd ..
pip install -r requirements.txt

รันเซิร์ฟเวอร์

    python manage.py runserver

📦 Dependencies

รายการไลบรารีทั้งหมดดูได้จากไฟล์:

requirements.txt

ตัวอย่าง:

Django
djangorestframework
python-dotenv

🧪 การทดสอบ (ถ้ามี)

python manage.py test

🔐 Environment Variables

แนะนำให้สร้างไฟล์ .env สำหรับข้อมูลสำคัญ เช่น

SECRET_KEY=your_secret_key
DEBUG=True
DATABASE_URL=...

🧑‍💻 ผู้พัฒนา

    GitHub: https://github.com/tanakorn911

    Project Repo: https://github.com/tanakorn911/rice_project

📄 License

โปรเจกต์นี้ใช้สัญญาอนุญาตแบบ MIT License

MIT License
🌾
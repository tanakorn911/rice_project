# Rice Mobile App (Expo + React Native + TypeScript)

MVP mobile-first client where **Sentinel-2 remains analytic core** and phone photos are used for field verification evidence.

## Quick start

```bash
cd mobile_app
npm install
npm run start
```

Set backend URL in `src/api/client.ts`.
